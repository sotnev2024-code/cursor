# Как запустить приложение

## Важно! 

**НЕ открывайте `index4.html` напрямую через файловую систему!**

## Правильный способ запуска:

### 1. Запустите Flask сервер:

```bash
python app.py
```

Вы увидите:
```
🚀 Сервер запущен на http://localhost:5000
📊 API доступен по адресу http://localhost:5000/api
📄 Фронтенд доступен по адресу http://localhost:5000/
```

### 2. Откройте браузер и перейдите по адресу:

```
http://localhost:5000/
```

Или:

```
http://localhost:5000/index4.html
```

## Почему нельзя открывать файл напрямую?

1. **CORS ошибки**: Браузер блокирует запросы к `http://localhost:5000` из `file://` протокола
2. **Относительные пути**: Пути к ресурсам (CSS, JS) могут работать неправильно
3. **API запросы**: Относительные пути `/api` не будут работать из `file://`

## Если вы все еще видите ошибку 404:

1. Убедитесь, что Flask сервер запущен
2. Проверьте, что вы открываете приложение через `http://localhost:5000/`
3. Откройте консоль браузера (F12) и проверьте, какие запросы отправляются
4. Убедитесь, что в `js/config.js` установлено:
   ```javascript
   API_BASE_URL: 'http://localhost:5000/api'
   ```

## Проверка работы API:

Откройте в браузере:
```
http://localhost:5000/api/categories
```

Должен вернуться JSON с категориями.

